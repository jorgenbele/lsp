(setq x 10) ;; global
(println "x is " x)
(setq x 7)    ;; global
(println "x is " x)

; define a function 'testfunc'
; which takes the arguments
; 'x', 'y' and 'z'
;;(defun testfunc (x y z)
;;  ; This is a function comment
;;  ; and describes what the function
;;  ; does
;;  (return (+ x y z))) ; return the sum
